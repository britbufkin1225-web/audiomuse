# Fixture archive record

A synthetic research note standing in for an institutional archive source. It exists so the
fixture registry can carry `evidence_class` and `retrieval` values and so the locator
resolves. Nothing here is canonical AudioMuse knowledge.
