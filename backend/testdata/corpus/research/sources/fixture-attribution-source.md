# Fixture attribution source

A synthetic research note standing in for a participant account. It is cited by a fixture
claim's `attribution` list and by nothing else, so the tests can tell an attribution-only
citation apart from an evidence citation. Nothing here is canonical AudioMuse knowledge.
